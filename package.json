{
  "name": "test-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run build.ts --serve",
    "build": "bun run build.ts",
    "broker": "bun run scripts/broker.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "peerjs": "^1.5.5"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "peer": "^1.0.2",
    "typescript": "^6.0.3"
  }
}